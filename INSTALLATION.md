# Инструкция по установке и использованию GitBook Wiki

## 📦 Что у вас есть

Структура вашей GitBook вики:

```
├── README.md                    # Главная страница
├── SUMMARY.md                   # Оглавление (структура)
├── book.json                    # Конфигурация GitBook
├── rules/                       # Правила сервера
│   ├── general.md              # Общие положения
│   ├── game-rules.md           # Игровые правила
│   ├── chat-rules.md           # Правила игрового чата
│   ├── discord-rules.md        # Правила Discord
│   └── punishments.md          # Система наказаний
├── guides/                      # Руководства
│   ├── getting-started.md      # Гайд для новичков
│   ├── commands.md             # Команды сервера
│   └── faq.md                  # Часто задаваемые вопросы
├── gameplay/                    # Игровые механики
│   ├── features.md             # Особенности сервера
│   └── worlds.md               # Миры и измерения
└── support/                     # Поддержка
    ├── contact.md              # Контакты
    ├── report.md               # Подача жалобы
    └── appeal.md               # Обжалование наказания
```

## 🚀 Способы использования

### Вариант 1: GitBook.com (Рекомендуется - самый простой)

1. **Регистрация:**
   - Перейдите на https://www.gitbook.com
   - Создайте бесплатный аккаунт

2. **Создание пространства:**
   - Нажмите "New Space"
   - Выберите "Import" → "GitHub"
   - Или выберите "Git Sync" для синхронизации

3. **Загрузка файлов:**
   - Создайте новый GitHub репозиторий
   - Загрузите все файлы вики в репозиторий
   - Подключите репозиторий к GitBook

4. **Публикация:**
   - GitBook автоматически создаст красивый сайт
   - Вы получите ссылку вида: https://your-space.gitbook.io

**Преимущества:**
- Очень просто
- Красивый интерфейс
- Автоматические обновления
- Бесплатно для публичных вики

### Вариант 2: GitHub Pages (Бесплатный хостинг)

1. **Создайте GitHub репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ-USERNAME/ВАШ-REPO.git
   git push -u origin main
   ```

2. **Настройте GitHub Pages:**
   - Settings → Pages
   - Source: main branch
   - Папка: / (root)

3. **Установите GitBook CLI локально:**
   ```bash
   npm install -g gitbook-cli
   gitbook build
   ```

4. **Загрузите собранные файлы:**
   - Папка `_book` содержит готовый сайт
   - Загрузите её на GitHub Pages

**Ваш сайт будет доступен по адресу:**
`https://ВАШ-USERNAME.github.io/ВАШ-REPO`

### Вариант 3: Локальная установка GitBook

1. **Установите Node.js:**
   - Скачайте с https://nodejs.org

2. **Установите GitBook CLI:**
   ```bash
   npm install -g gitbook-cli
   ```

3. **Инициализация (если нужно):**
   ```bash
   cd /путь/к/вашей/вики
   gitbook init
   ```

4. **Запуск локального сервера:**
   ```bash
   gitbook serve
   ```
   Откройте http://localhost:4000

5. **Сборка статического сайта:**
   ```bash
   gitbook build
   ```
   Готовый сайт будет в папке `_book/`

## ✏️ Редактирование контента

### Базовый Markdown синтаксис:

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3

**Жирный текст**
*Курсив*

* Список
* Пункт 2

1. Нумерованный
2. Список

[Ссылка](https://example.com)

![Изображение](путь/к/изображению.png)

> Цитата

`код`

​```
Блок кода
​```
```

### Добавление новых страниц:

1. Создайте новый `.md` файл
2. Добавьте ссылку в `SUMMARY.md`:
   ```markdown
   * [Название страницы](путь/к/файлу.md)
   ```

### Добавление изображений:

1. Создайте папку `images/`
2. Положите туда изображения
3. Используйте в файлах:
   ```markdown
   ![Описание](images/название.png)
   ```

## 🔧 Настройка book.json

Откройте `book.json` и измените:

```json
{
  "title": "ВАШЕ НАЗВАНИЕ",
  "description": "ВАШЕ ОПИСАНИЕ",
  "pluginsConfig": {
    "edit-link": {
      "base": "https://github.com/ВАШ-USERNAME/ВАШ-REPO/edit/main"
    },
    "github": {
      "url": "https://github.com/ВАШ-USERNAME/ВАШ-REPO"
    }
  },
  "links": {
    "sidebar": {
      "Discord сервер": "https://discord.gg/ВАШ-INVITE",
      "Наш сайт": "https://ВАШ-САЙТ.com"
    }
  }
}
```

## 📝 Что нужно изменить под ваш сервер

В файлах есть метки `[укажите...]` которые нужно заменить:

1. **README.md:**
   - Ссылки на Discord, сайт, заявки

2. **guides/getting-started.md:**
   - IP адрес сервера
   - Стартовый набор (если есть)
   - Команды (если отличаются)

3. **guides/commands.md:**
   - Список доступных команд
   - Лимиты на дома
   - VIP команды

4. **guides/faq.md:**
   - Версия Minecraft
   - IP адрес
   - Информация о донате
   - Средний онлайн

5. **gameplay/features.md:**
   - Информация об экономике
   - Список плагинов
   - Границы мира

6. **support/contact.md:**
   - Email поддержки
   - Ссылки на соцсети

## 🎨 Кастомизация

### Плагины GitBook:

В `book.json` можно добавить плагины:

```json
"plugins": [
  "theme-comscore",        // Красивая тема
  "accordion",             // Складывающиеся блоки
  "image-captions",        // Подписи к картинкам
  "youtube",               // Встраивание YouTube
  "page-toc",              // Оглавление страницы
  "-sharing"               // Убрать кнопки соц. сетей
]
```

Установка плагинов:
```bash
gitbook install
```

### Изменение темы:

Можно установить другие темы, например:
```json
"plugins": ["theme-default", "theme-comscore"]
```

## 🌐 Публикация

### GitBook.com:
- Автоматически обновляется при push в GitHub
- Поддерживает кастомные домены

### GitHub Pages:
```bash
gitbook build
cd _book
git init
git add .
git commit -m "Deploy"
git push -f https://github.com/USER/REPO.git main:gh-pages
```

### Собственный хостинг:
- Соберите: `gitbook build`
- Загрузите папку `_book/` на ваш хостинг
- Настройте веб-сервер (nginx/apache)

## 🔍 Советы

1. **Используйте Git для версионирования**
   ```bash
   git add .
   git commit -m "Обновление правил"
   git push
   ```

2. **Регулярно обновляйте контент**
   - Добавляйте новые разделы
   - Обновляйте правила
   - Отвечайте на частые вопросы в FAQ

3. **Просите обратную связь от игроков**
   - Что непонятно?
   - Чего не хватает?
   - Что можно улучшить?

## ❓ Проблемы и решения

**GitBook не собирается:**
```bash
npm cache clean --force
npm install -g gitbook-cli
gitbook install
```

**Ошибки с плагинами:**
```bash
rm -rf node_modules _book
npm install
gitbook install
```

**Не работает на GitBook.com:**
- Проверьте формат SUMMARY.md
- Убедитесь что все ссылки правильные
- Проверьте синтаксис Markdown

## 📚 Полезные ссылки

- GitBook документация: https://docs.gitbook.com
- Markdown Guide: https://www.markdownguide.org
- GitHub Pages: https://pages.github.com
- Плагины GitBook: https://plugins.gitbook.com

## 🎉 Готово!

Ваша Wiki готова к использованию! Выберите удобный способ публикации и делитесь ссылкой с игроками.

**Удачи с вашим сервером!** 🎮
